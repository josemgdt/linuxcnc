
Esta es un test de la función hm2_register() en el controlador hostmot2(9).

Crea una falsa tarjeta AnyIO (usando el controlador hm2_test) con un 
"archivo de registro" estático que contiene un patrón de prueba. La falsa
tarjeta AnyIO se pasa a hm2_register(), y vemos si entra en pánico.

